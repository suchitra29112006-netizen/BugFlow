services:
  # BugFlow Unified Full-Stack Application (React UI + FastAPI Backend in 1 Service)
  - type: web
    name: bugflow
    runtime: python
    plan: free
    buildCommand: cd frontend && npm install && npm run build && cd ../backend && pip install -r requirements.txt
    startCommand: cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
